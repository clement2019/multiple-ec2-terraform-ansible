# multiple-ec2-terraform-ansible
o provision and configure multiple AWS EC2 instances using Terraform and Ansible, the general approach is to use Terraform for infrastructure provisioning and Ansible for configuration management, connecting the two by generating a dynamic Ansible inventory from Terraform's outputs. 
